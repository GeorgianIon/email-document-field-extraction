# ════════════════════════════════════════════════════════════════
# Requirements for the local pipeline (Paradigm 1), data generation,
# and the robustness / augmentation experiments.
#
# Paradigm 2 (Qwen2.5-VL) and Paradigm 3 (Donut) run on GPU in the
# Colab notebooks under notebooks/ and install their own dependencies.
# The Streamlit demo has its own demo_app/requirements.txt.
# ════════════════════════════════════════════════════════════════

# ── Core / data generation ──
numpy>=1.24
reportlab>=4.0.0          # synthetic PDF/PNG document rendering
Pillow>=10.0.0            # image handling

# ── Classical pipeline (P1) ──
scikit-learn>=1.3.0       # TF-IDF baseline, metrics
pytesseract>=0.3.10       # OCR wrapper (requires the Tesseract engine installed)
pypdfium2>=4.0.0          # PDF -> image rasterization

# ── Intent classifier (DistilBERT) ──
transformers>=4.38.0
datasets>=2.16.0
torch>=2.1.0
accelerate>=0.26.0

# ── Robustness / augmentation experiments ──
nlpaug>=1.1.11            # text corruption
opencv-python-headless>=4.8.0
albumentations>=1.3.0

# ── Real-email demo (demo_pipeline.py) ──
extract-msg>=0.45.0      # parse Outlook .msg files
