# Core
streamlit>=1.30.0
pandas>=2.0.0
requests>=2.28.0

# Outlook integration (Windows-only)
pywin32>=306; platform_system == "Windows"
extract-msg>=0.45.0

# Document processing (for P1 inference)
pypdfium2>=4.0.0
Pillow>=10.0.0
pytesseract>=0.3.10

# Report generation
# weasyprint>=60.0  # Optional, for PDF export. Requires GTK on Windows.

# Helper
python-dateutil>=2.8.0
